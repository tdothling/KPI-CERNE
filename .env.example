# URL da function serverless (vercel-api/) que gera a análise de IA do relatório.
# Preencha com a URL real depois do deploy no Vercel (ex: https://kpi-cerne-report-api.vercel.app/api/generate-report-narrative)
# e copie este arquivo para .env.local (git-ignorado).
VITE_REPORT_API_URL=
